includes:
    - ../phpstan.neon
parameters:
    # keep in sync with ../phpstan.neon: a higher level here makes the pre-push
    # hook block on pre-existing errors in files a push merely touches
    level: 1
    tmpDir: /tmp/phpstan/GoogleAnalyticsImporter/modified
