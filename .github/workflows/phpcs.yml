name: PHPCS check

on: pull_request

permissions:
  actions: read
  checks: read
  contents: read
  deployments: none
  issues: read
  packages: none
  pull-requests: read
  repository-projects: none
  security-events: none
  statuses: read

jobs:
  phpcs:
    name: PHPCS
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: false
          persist-credentials: false
      - name: Install dependencies
        run: cd ../../;composer install --dev --prefer-dist --no-progress --no-suggest
      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '7.4'
          tools: cs2pr
      - name: Check PHP code styles
        id: phpcs
        run: cd ../../;./vendor/bin/phpcs --report-full -standard=plugins/GoogleAnalyticsImporter/phpcs.xml --report-checkstyle=./phpcs-report.xml plugins/GoogleAnalyticsImporter
      - name: Show PHPCS results in PR
        if: ${{ always() && steps.phpcs.outcome == 'failure' }}
        run: cs2pr ./phpcs-report.xml --prepend-filename
